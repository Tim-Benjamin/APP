<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_gray">

    <!-- Scrollable Content -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="@dimen/spacing_xxl">

            <!-- Stop Selector Card -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/card_stop_selector"
                style="@style/Widget.CampusRide.Card"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/margin_default">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="@dimen/padding_default">

                    <!-- Label -->
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/your_current_stop"
                        android:textSize="@dimen/text_size_small"
                        android:textColor="@color/text_secondary"
                        android:layout_marginBottom="@dimen/spacing_s" />

                    <!-- Stop Spinner -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <ImageView
                            android:layout_width="@dimen/icon_size_medium"
                            android:layout_height="@dimen/icon_size_medium"
                            android:src="@drawable/ic_map"
                            android:tint="@color/ucc_blue"
                            android:contentDescription="@string/your_current_stop"
                            android:layout_marginEnd="@dimen/spacing_m" />

                        <Spinner
                            android:id="@+id/spinner_stops"
                            android:layout_width="0dp"
                            android:layout_height="@dimen/input_height"
                            android:layout_weight="1"
                            android:spinnerMode="dropdown" />

                    </LinearLayout>

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- Map Card -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/card_map"
                style="@style/Widget.CampusRide.Card"
                android:layout_width="match_parent"
                android:layout_height="@dimen/map_height_default"
                android:layout_marginStart="@dimen/margin_default"
                android:layout_marginEnd="@dimen/margin_default"
                android:layout_marginBottom="@dimen/margin_default">

                <!-- Map Fragment Container -->
                <FrameLayout
                    android:id="@+id/map_container"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <!-- Placeholder for Google Maps Fragment -->
                    <fragment
                        android:id="@+id/map_fragment"
                        android:name="com.google.android.gms.maps.SupportMapFragment"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent" />

                    <!-- My Location Button -->
                    <com.google.android.material.floatingactionbutton.FloatingActionButton
                        android:id="@+id/fab_my_location"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="top|end"
                        android:layout_margin="@dimen/margin_default"
                        android:src="@drawable/ic_my_location"
                        android:contentDescription="@string/my_location"
                        app:fabSize="mini"
                        app:backgroundTint="@color/white"
                        app:tint="@color/ucc_blue"
                        app:elevation="@dimen/fab_elevation" />

                </FrameLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- Section Header -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/nearby_shuttles"
                android:textAppearance="@style/TextAppearance.CampusRide.Heading"
                android:layout_marginStart="@dimen/margin_default"
                android:layout_marginEnd="@dimen/margin_default"
                android:layout_marginTop="@dimen/margin_small"
                android:layout_marginBottom="@dimen/margin_medium" />

            <!-- Shuttle List RecyclerView -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rv_shuttles"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                android:paddingStart="@dimen/padding_small"
                android:paddingEnd="@dimen/padding_small" />

            <!-- Empty State -->
            <LinearLayout
                android:id="@+id/layout_empty_state"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:padding="@dimen/empty_state_padding"
                android:visibility="gone">

                <ImageView
                    android:layout_width="@dimen/empty_state_icon_size"
                    android:layout_height="@dimen/empty_state_icon_size"
                    android:src="@drawable/ic_logo_splash"
                    android:alpha="0.3"
                    android:contentDescription="@string/no_shuttles" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/no_shuttles"
                    android:textAppearance="@style/TextAppearance.CampusRide.Subheading"
                    android:layout_marginTop="@dimen/spacing_l" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/no_shuttles_desc"
                    android:textAppearance="@style/TextAppearance.CampusRide.BodySecondary"
                    android:textAlignment="center"
                    android:layout_marginTop="@dimen/spacing_s" />

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <!-- Loading Overlay -->
    <FrameLayout
        android:id="@+id/loading_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/overlay_light"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true">

        <ProgressBar
            android:layout_width="@dimen/progress_size_large"
            android:layout_height="@dimen/progress_size_large"
            android:layout_gravity="center"
            android:indeterminateTint="@color/ucc_blue" />

    </FrameLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>