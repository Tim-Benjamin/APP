<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_gray">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/padding_default">

            <!-- Header Card -->
            <com.google.android.material.card.MaterialCardView
                style="@style/Widget.CampusRide.Card"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_default"
                app:cardBackgroundColor="@color/ucc_blue">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="@dimen/padding_default">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/favorites_title"
                        android:textAppearance="@style/TextAppearance.CampusRide.Heading"
                        android:textColor="@color/white" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Quick access to your most used stops"
                        android:textAppearance="@style/TextAppearance.CampusRide.Body"
                        android:textColor="@color/white"
                        android:alpha="0.9"
                        android:layout_marginTop="@dimen/spacing_xs" />

                </LinearLayout>

            </com.google.android.material.card.MaterialCardView>

            <!-- Favorite Stops List -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rv_favorite_stops"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                tools:listitem="@layout/item_favorite_stop" />

            <!-- Empty State -->
            <LinearLayout
                android:id="@+id/layout_empty_favorites"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:padding="@dimen/empty_state_padding"
                android:visibility="gone"
                tools:visibility="visible">

                <ImageView
                    android:layout_width="@dimen/empty_state_icon_size"
                    android:layout_height="@dimen/empty_state_icon_size"
                    android:src="@drawable/ic_star"
                    android:tint="@color/text_hint"
                    android:alpha="0.3"
                    android:contentDescription="@string/no_favorites" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/no_favorites"
                    android:textAppearance="@style/TextAppearance.CampusRide.Subheading"
                    android:layout_marginTop="@dimen/spacing_l" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/no_favorites_desc"
                    android:textAppearance="@style/TextAppearance.CampusRide.BodySecondary"
                    android:textAlignment="center"
                    android:layout_marginTop="@dimen/spacing_s"
                    android:paddingStart="@dimen/padding_large"
                    android:paddingEnd="@dimen/padding_large" />

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>